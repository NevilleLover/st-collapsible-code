/* Collapsible Code Blocks Styles */
pre {
    transition: max-height 0.3s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
}

pre.collapsed {
    max-height: 2.5em !important;
}

pre::after {
    content: '▼ Click to collapse';
    position: absolute;
    top: 5px;
    right: 35px; /* Space for copy button */
    background: rgba(0, 0, 0, 0.9);
    color: #9945ff;
    padding: 2px 8px;
    font-size: 0.8em;
    z-index: 10;
    font-weight: bold;
    border-radius: 3px;
    pointer-events: none; /* Don't interfere with clicks */
}

pre.collapsed::after {
    content: '▶ Click to expand';
}

/* Don't change cursor on copy button */
pre .fa-copy {
    cursor: pointer !important;
}